# Uncomment the next line to define a global platform for your project
 platform :ios, '8.0'

target 'DynamicCellHeight' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for DynamicCellHeight
      pod 'SDWebImage', '~> 4.0'
      pod 'MBProgressHUD'



  target 'DynamicCellHeightTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DynamicCellHeightUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
